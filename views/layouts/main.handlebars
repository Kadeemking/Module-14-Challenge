<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>The Tech Blog</title>
  <link rel="stylesheet" href="/css/jass.css">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body class="bg-info">
  <div class="flex-column justify-space-around">
    <header class=" justify-space-between align-center p-2 pb-4 bg-primary">
      
      <nav>
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <a href="/" class="mx-3 text-info">Home</a>
        <a href="/view-posts" class="mx-3 text-info">Dashboard</a> 
        <button class="no-button mx-3 text-info" id="logout">Logout</button>
        {{else}}
        <a href="/login" class="mx-3 text-info">Login</a>
        {{/if}}
      </nav>
      <div class="mt-3 text-primary" id="title">
      <h1>
        The Tech Blog
      </h1>
      </div>
    </header>
    <main class="container container-fluid mt-5 ">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    <footer>
      
    </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
