  <div class="card">
  <div class="card-header">
    <h3 class="text-info">Edit Post:</h3>
</div>
  <form class="form edit-form p-3">
    <div class="form-group">
      <h4 for="project-name" class="text-primary">Title:</h4>
      <input id="name-edit" class="form-input" value="{{name}}" />
    </div>
    <div class="form-group"></div>
    <h4 for="project-name" class="text-primary">Content:</h4>
    <textarea id="description-edit" class="form-input">{{description}}</textarea>


{{!-- <p>Created by {{user.name}} on {{format_date date_created}}</p> --}}
    <p class="mt-4">
      <button data-id="{{id}}" class="btn btn-primary w-100">Update Post</button>
    </p>
    <p>
      <button data-id="{{id}}" class="btn btn-primary delete-btn w-100">Delete Post</button>
    </p>

    </div>


    
  </form>
  <script src="/js/editPosts.js"></script>